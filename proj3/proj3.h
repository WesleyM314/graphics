#define WSIZE 1024

void setCurPoint(int x, int y);

// A struct that can grow dynamically to handle unknown amounts of data
// typedef struct
// {
// 	GLfloat *items;
// 	int capacity;
// 	int total;
// } floatList;

// void listNew(floatList *l);
// void listResize(floatList *list, int capacity);
// void listPush(floatList *list, GLfloat item);
